VITE_API_BASE_URL=http://localhost:8080

# Mapbox GL JS token for the dashboard's spend map (docs/07-tech-stack.md). Get one free at
# mapbox.com/account/access-tokens. Left unset, the map renders a "Map unavailable" fallback
# instead of crashing the dashboard.
VITE_MAPBOX_TOKEN=
